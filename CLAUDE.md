# Claude Code Project Adapter

## Continuation contract (2026-08-04)

The shared truth is the checked-out Git branch and this repository's Markdown, not a
previous Claude or Codex chat. Open `C:\AI-Workspace\Projects\Star-Sticker-Parade`
on `codex/ssp-visual-proof`, then read `AGENTS.md`, `docs/PROJECT_STATE.md`,
`docs/WORK_PACKETS.md`, `ASSET_UPGRADE_PLAN.md`, and `CLAUDE_BROWSER_HANDOFF.md`.
`AGENTS.md` and `ASSET_UPGRADE_PLAN.md` control current visual direction; historical
notes in the browser handoff do not override them.

Do not work from `main` or the older `claude/star-sticker-parade-dev-jcdfst` branch
without an explicit reconciliation decision. Keep one bounded work packet at a time,
preserve the five minigames/save/parade/mobile boot unless the packet says otherwise,
and commit its handoff with the implementation so either Claude or Codex can resume.

Before work, read `../../Brain/CLAUDE.md`, this project's `AGENTS.md` (status, branch note, and source-of-truth list), `ASSET_UPGRADE_PLAN.md`, and `CLAUDE_BROWSER_HANDOFF.md`.

This is active production on the `codex/ssp-visual-proof` branch, not `main`'s stale placeholder scaffold — see `AGENTS.md` for the full branch-divergence note. Do not treat `main` as current for this project.
